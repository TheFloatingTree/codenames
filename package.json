{
  "name": "codenames",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "cd server && yarn start",
    "build": "cd client/ && yarn install && yarn build && cd ../server/ && yarn install && yarn build",
    "dev": "run-p dev-server dev-client",
    "dev-server": "cd server && yarn dev",
    "dev-client": "cd client && yarn dev"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
